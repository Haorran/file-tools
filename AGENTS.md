# AGENTS.md — Codex 代理规则

## 改动流程

**所有代码改动必须先给方案，等用户确认后再动手。**

1. 分析问题，列出 2-3 个可选方案
2. 每个方案说明优缺点
3. 推荐一个方案并说明理由
4. 等用户选择后再执行
5. 简单拼写错误、微小修复（<3 行）可直接动手

## 打包

仅在用户明确说"打包"时执行 `make release`，其他时候不自动打包。

## 版本号

`make release` 自动递增版本号，无需手动改 manifest。
